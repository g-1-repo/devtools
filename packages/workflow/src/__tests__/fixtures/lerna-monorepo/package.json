{
  "name": "lerna-monorepo-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "Test fixture for Lerna monorepo",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "clean": "lerna clean",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^6.0.0",
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**"
    ]
  }
}